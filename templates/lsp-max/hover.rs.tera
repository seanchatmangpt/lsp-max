//! Handler: textDocument/hover — {{ law_status }}
//! Generated by ggen lsp-max pack — edit domain.ttl to change law_status
{% if law_status == "ADMITTED" %}
//! ADMITTED: receipt attached. This file was NOT auto-regenerated after admission.
{% else %}
//! CANDIDATE: transcript required for ADMITTED
//! To promote: attach receipt triple in schema/domain.ttl, then `ggen sync`
{% endif %}

use lsp_max::jsonrpc::Result;
use lsp_types_max::*;

/// {{ law_status }}: textDocument/hover
///
/// Provides hover information (type hints, documentation) for symbols at cursor position.
/// Dispatched via FirstSuccess routing through the compositor.
pub async fn text_document_hover(params: HoverParams) -> Result<Option<Hover>> {
{% if law_status == "ADMITTED" %}
    // ADMITTED — implement here
    let _ = params;
    todo!("implement textDocument/hover")
{% else %}
    // CANDIDATE — stub returns safe default
    let _ = params;
    Ok(None)
{% endif %}
}
