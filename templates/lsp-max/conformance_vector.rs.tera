//! ConformanceVector — AUTO-GENERATED by ggen lsp-max pack
//! Source: schema/domain.ttl + queries/conformance.sparql
//! Edit the .ttl, run `ggen sync` — never edit this file directly

use lsp_max::max_protocol::ConformanceVector;

pub fn conformance_vector() -> ConformanceVector {
    {% set grouped = results | group_by(attribute='law_status') %}
    ConformanceVector {
        admitted: vec![
            {% for m in grouped.ADMITTED | default(value=[]) %}
            "{{ m.method_name }}",
            {% endfor %}
        ],
        refused: vec![
            {% for m in grouped.REFUSED | default(value=[]) %}
            "{{ m.method_name }}",
            {% endfor %}
        ],
        unknown: vec![
            {% for m in grouped.UNKNOWN | default(value=[]) %}
            "{{ m.method_name }}",
            {% endfor %}
        ],
        ..Default::default()
    }
}
