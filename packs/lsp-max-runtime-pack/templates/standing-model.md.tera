{%- set states = sparql(query='PREFIX lsp: <https://chatmangpt.com/ns/lsp-max#> SELECT ?rank ?name ?definition WHERE { ?s a lsp:StandingState ; lsp:rank ?rank ; lsp:name ?name ; lsp:definition ?definition . } ORDER BY ?rank') -%}
{%- set obligations = sparql(query='PREFIX lsp: <https://chatmangpt.com/ns/lsp-max#> PREFIX skos: <http://www.w3.org/2004/02/skos/core#> SELECT ?id ?label WHERE { ?o a lsp:EvidenceObligation ; lsp:obligationId ?id ; skos:prefLabel ?label . } ORDER BY ?id') -%}
<!-- @generated by ggen; edit packs/lsp-max-runtime-pack/ontology.ttl or this template. -->
# lsp-max Standing Model

| Rank | State | Admission meaning |
|---:|---|---|
{% for state in states -%}
| {{ state.rank }} | `{{ state.name }}` | {{ state.definition }} |
{% endfor %}

## Crown rule

`ALIVE` is lawful only when all of the following are observed and bound to the same admitted input closure:

{% for obligation in obligations -%}
- {{ obligation.label }} (`{{ obligation.id }}`)
{% endfor %}

`PARTIAL_ALIVE` may describe one closed checkpoint. It may not be promoted into crown standing. `UNKNOWN` does not mean refusal. `UNSUPPORTED` does not mean execution failure. `BUILD_BROKEN` requires an observed build failure.
